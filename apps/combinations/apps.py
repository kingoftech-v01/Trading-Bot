from django.apps import AppConfig


class CombinationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.combinations'
    verbose_name = 'Trading Combinations'
